var totalCart=100;
var cash=300;
var card=true;

if(cash>=totalCart){
    console.log(`Pay with cash!`);
}else if(card){
    console.log(`Invalid payment`);
}else{
    console.log(`Invalid `)
}

var logged=false;

if(logged){
    consol.log(`You are already logged`);
}else{
    console.log(`Please, login to the system`);
}

document.write(`You have to pay ${totalCart}`);

